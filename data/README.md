# Datos

El proyecto utiliza el dataset Iris incluido en Scikit-learn.

No hace falta almacenar un archivo de datos dentro del repositorio, ya que el dataset se carga directamente desde el notebook mediante `load_iris`.
